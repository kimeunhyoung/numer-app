PORT=3000
# QR/enter 기본(로컬). Render: 주소는 RENDER_EXTERNAL_URL이 자동. QR이 numerology-app 등 옛 주소면 대시보드에서 잘못된 APP_BASE_URL을 삭제.
APP_BASE_URL=http://127.0.0.1:3000
# QR에만 쓸 공개 URL (선택, 커스텀 도메인). 미설정이면 Render 실제 URL 사용.
# PUBLIC_QR_BASE_URL=
APP_PASSWORD=888
JWT_SECRET=replace-with-a-long-random-secret
